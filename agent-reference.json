{
  "$schema": "https://unpkg.com/agent-reference/schema/agent-reference.schema.json",
  "packages": {
    "typescript": {
      "version": "5.9.3",
      "description": "Read when the compiler disagrees with the code and the error text alone does not say why. src/compiler/checker.ts carries the message strings; tests/cases/ has a reproduction for most of them"
    }
  }
}
