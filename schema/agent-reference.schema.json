{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://unpkg.com/agent-reference/schema/agent-reference.schema.json",
  "title": "agent-reference config",
  "description": "Desired state for the local reference source an agent can read. Lives at agent-reference.json (committed) or agent-reference.local.json (machine-specific, gitignored). Both are read as JSON with comments (// and /* */) and trailing commas, so a note beside an entry is part of the format; keep any the file already carries when editing it.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "URL of this schema. Optional; ignored at runtime."
    },
    "packages": {
      "type": "object",
      "description": "npm ecosystem packages to materialize, keyed by package name.",
      "additionalProperties": {
        "oneOf": [
          {
            "$ref": "#/$defs/packageVersion"
          },
          {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "version"
            ],
            "properties": {
              "version": {
                "$ref": "#/$defs/packageVersion"
              },
              "ref": {
                "type": "string",
                "description": "Commit, tag, or branch to check out, overriding automatic version resolution. Use this when a repository's tagging scheme cannot be guessed; a pinned ref always wins."
              },
              "repository": {
                "type": "string",
                "description": "Git remote to use when registry metadata has none or points at the wrong repo. github:owner/repo, a git URL, or file:../repo. Setting both repository and ref skips the registry entirely, which is how unpublished or private packages work."
              },
              "directory": {
                "type": "string",
                "description": "Package subdirectory within the repository, for monorepos where it cannot be detected."
              },
              "description": {
                "$ref": "#/$defs/description"
              }
            }
          }
        ]
      },
      "examples": [
        {
          "prettier": "3.6.2",
          "zod": {
            "version": "3.25.0",
            "description": "Schema shapes we mirror in our own validators"
          }
        }
      ]
    },
    "paths": {
      "type": "object",
      "description": "Files and folders already on this machine that should show up as agent references, keyed by shorthand name. Whether a path is a file or a folder is read from disk, not declared here.",
      "additionalProperties": {
        "oneOf": [
          {
            "type": "string",
            "minLength": 1,
            "description": "Path to a file or folder. Relative paths resolve from the project root; ~/ resolves from the home directory."
          },
          {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "path"
            ],
            "properties": {
              "path": {
                "type": "string",
                "minLength": 1
              },
              "description": {
                "$ref": "#/$defs/description"
              }
            }
          }
        ]
      },
      "examples": [
        {
          "design-notes": "./references/design-notes",
          "api-docs": {
            "path": "../platform/docs",
            "description": "Source of truth for endpoint contracts"
          },
          "release-checklist": {
            "path": "~/notes/release-checklist.md",
            "description": "One file rather than a folder; both are paths"
          }
        }
      ]
    },
    "git": {
      "type": "object",
      "description": "Arbitrary git repositories to materialize, keyed by shorthand name.",
      "additionalProperties": {
        "oneOf": [
          {
            "type": "string",
            "minLength": 1,
            "description": "github:owner/repo#ref, a git URL with an optional #ref, or file:../repo#ref."
          },
          {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "repository"
            ],
            "properties": {
              "repository": {
                "type": "string",
                "minLength": 1,
                "description": "github:owner/repo, a git URL, or file:../repo."
              },
              "ref": {
                "type": "string",
                "description": "Branch, tag, or commit. Defaults to the repository default branch."
              },
              "directory": {
                "type": "string",
                "minLength": 1,
                "description": "Subtree of the checkout worth reading, for a repository that is a monorepo. Resolved against the checkout on every command, so a directory upstream has moved is reported rather than silently ignored."
              },
              "description": {
                "$ref": "#/$defs/description"
              }
            }
          }
        ]
      },
      "examples": [
        {
          "typescript": "github:microsoft/TypeScript#main",
          "design-system": {
            "repository": "github:acme/monorepo",
            "ref": "v4",
            "directory": "packages/design-system"
          }
        }
      ]
    },
    "allImporters": {
      "type": "boolean",
      "deprecated": true,
      "description": "No longer used. Every workspace importer is read, and a name installed at several versions is reported rather than picked."
    },
    "registry": {
      "type": "string",
      "description": "npm registry base URL. Defaults to https://registry.npmjs.org."
    },
    "cacheDir": {
      "type": "string",
      "description": "Override the machine-wide store location for bare repositories and shared worktrees."
    },
    "sets": {
      "type": "array",
      "description": "Labeled lists of references: a description saying what the collection is for, with members declared inline. Member names derive from the path or repository basename. The same reference may appear in several sets.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "description"
        ],
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Optional short handle for CLI selection (--set <name>). The description is the display heading either way."
          },
          "description": {
            "type": "string",
            "minLength": 1,
            "description": "What this collection is for; doubles as the heading in status output."
          },
          "paths": {
            "type": "array",
            "items": {
              "oneOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Path to a file or folder."
                },
                {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "path"
                  ],
                  "properties": {
                    "path": {
                      "type": "string",
                      "minLength": 1
                    },
                    "name": {
                      "type": "string",
                      "minLength": 1,
                      "description": "Explicit reference name, overriding the one derived from the path or repository basename."
                    },
                    "description": {
                      "$ref": "#/$defs/description"
                    }
                  }
                }
              ]
            }
          },
          "git": {
            "type": "array",
            "items": {
              "oneOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "github:owner/repo#ref, a git URL with an optional #ref, or file:../repo#ref."
                },
                {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "repository"
                  ],
                  "properties": {
                    "repository": {
                      "type": "string",
                      "minLength": 1
                    },
                    "ref": {
                      "type": "string"
                    },
                    "directory": {
                      "type": "string",
                      "minLength": 1,
                      "description": "Subtree of the checkout worth reading, for a repository that is a monorepo. Resolved against the checkout on every command, so a directory upstream has moved is reported rather than silently ignored."
                    },
                    "name": {
                      "type": "string",
                      "minLength": 1,
                      "description": "Explicit reference name, overriding the one derived from the path or repository basename."
                    },
                    "description": {
                      "$ref": "#/$defs/description"
                    }
                  }
                }
              ]
            }
          },
          "packages": {
            "type": "array",
            "items": {
              "oneOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Package name; the version follows the lockfile."
                },
                {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "name"
                  ],
                  "properties": {
                    "name": {
                      "type": "string",
                      "minLength": 1
                    },
                    "version": {
                      "$ref": "#/$defs/packageVersion"
                    },
                    "ref": {
                      "type": "string"
                    },
                    "repository": {
                      "type": "string"
                    },
                    "directory": {
                      "type": "string"
                    },
                    "description": {
                      "$ref": "#/$defs/description"
                    }
                  }
                }
              ]
            }
          }
        }
      },
      "examples": [
        [
          {
            "description": "Documentation sources to read before writing docs",
            "paths": [
              "./references/design-notes",
              {
                "path": "../platform/docs",
                "name": "api-docs",
                "description": "Source of truth for endpoint contracts"
              }
            ],
            "git": [
              "github:acme/design-system#v4"
            ]
          }
        ]
      ]
    }
  },
  "$defs": {
    "packageVersion": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+(?:[-+][0-9A-Za-z.-]+)?$",
      "description": "An exact published version, such as \"3.25.0\". Ranges, dist-tags, and \"installed\" are rejected: a config entry is a fixed coordinate that has to mean the same thing on every machine and next month. Run `agent-reference versions <name>` to see what this project installs."
    },
    "description": {
      "type": "string",
      "description": "Free-form note explaining what this reference is for. Shown to agents in `agent-reference status`."
    }
  }
}
