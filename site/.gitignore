node_modules

# 568 KB of generated Cloudflare types for an Env this Worker never reads: it
# has no bindings. Regenerate with `pnpm cf-typegen` if that changes.
worker-configuration.d.ts
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

.dev.vars*
!.dev.vars.example
!.env.example
